# ARCHITECTURE.md — <Project Name>

Generated by `tidyfactor-next` → `init` command. Every later command
in this track reads this file before generating any code or making any
recommendation. Keep it current — it is the single source of truth for
decisions that would otherwise drift between sessions or between commands.

> **Rule for all commands**: If a field below is unfilled (`<placeholder>`),
> do NOT silently decide it. Surface the gap to the developer and route back
> to `init` to fill it before proceeding.

---

## Locked platform (not chosen per-project)

- Next.js 16, App Router, React 19, TypeScript strict
- Supabase (Postgres, Auth, Storage, Edge Functions, Realtime as needed)
- Tenant isolation: shared schema, `tenant_id` + Postgres RLS

---

## Chosen once — this project (filled during `init` interview)

- **Data/query layer:** `<Supabase JS client + raw SQL/RLS | Drizzle ORM + Supabase | Prisma + Supabase>`
- **Tenant resolution strategy:** `<subdomain | custom domain | path prefix | session claim>`
- **Auth provider + methods:** `<Supabase Auth email/password | magic link | OAuth: [providers]>`
- **Role model:** `<fixed roles (owner/admin/member) | custom RBAC | ABAC>`
- **Deployment target:** `<Vercel | self-hosted | other>`
- **Environments:** `<dev / staging / prod — how Supabase projects map to each>`

---

## Domain boundaries

> List top-level business domains and their ownership scope.
> Filled during `init` based on the product the developer describes.

```
Domain: <name>
  Scope:    <what it owns>
  Tables:   <list>
  Tenant-scoped: yes | no (if no, document why in ADR log)
```

---

## Performance context (read by `perf` command)

> Filled as the project grows. The `perf` command reads this section before
> running any audit phase. If unfilled, `perf` will run a discovery phase first.

- **Known bottlenecks:** `<none identified | [description]>`
- **Storage strategy:** `<project filesystem | Supabase Storage | S3 | CDN>`
- **Large media in dev tree:** `<yes — path: X | no>`
- **Active perf baseline:** `<none | see last-perf-audit.md>`
- **Last perf audit date:** `<none | YYYY-MM-DD>`
- **Last perf audit git commit:** `<none | SHA>`

---

## ADR log

> Append-only. Each entry captures a significant architectural decision so it
> is never re-debated silently in a future session.

Format:
```
[YYYY-MM-DD] <Decision title>
  Decision:   <what was decided>
  Alternatives considered: <what else was evaluated>
  Reason:     <why this option was chosen>
  Command:    <which skill command made or surfaced this decision>
```

---

## Open risks / tech debt

> Tracked as they are identified by commands (`audit`, `perf`, `incident`, `rls`).
> Each entry: source command, risk description, priority (P0–P3), status.

```
[Source: perf | rls | audit | incident]
Risk:     <description>
Priority: P0 | P1 | P2 | P3
Status:   Open | Addressed | Accepted
```
